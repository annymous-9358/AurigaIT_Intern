React (Front-End Only)
The task is to create a list of users.
User will have firstName and lastName
Add Tasks
An input field and an “Add” button to insert a new user.
List Tasks
Display all tasks with basic styling (don’t focus on UI).
Update Tasks
An “Edit” button to modify user name.
Delete Tasks
A “Delete” button to remove user from the list.
Search
A search bar to filter user in real time.
For now, store user in React state (no backend). 